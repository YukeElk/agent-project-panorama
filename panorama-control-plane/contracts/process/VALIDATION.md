**P0 实施与验证记录 · 2026-09-14**

P0 已完成四类公开 Schema、共享定义、状态合同、六条基础必备与四条条件必备规则、Structure/旧格式映射、固定上游来源及两项目七组合同样本。实现范围为 `contracts/process/`、`test/process/`、测试命令注册和文档；没有增加应用运行时模块或目标项目接入。

| 检查 | 结果 | 证据与含义 |
|---|---|---|
| Draft 2020-12 Schema 编译 | 5/5 通过 | 四类入口及共享定义；本地 Registry，禁止网络取回引用 |
| 规范包与公开样本格式 | 22 次校验通过，涉及 17 份不同 JSON | 一份规则包、两份项目配置、七份 Receipt、七份 Assessment；重复项目配置随各场景检查 |
| 非法或内部矛盾数据 | 30 个负例全部拒绝 | 日期、路径、执行退出码、必备项、未知/过期与整体状态、样本身份、开始基线等 |
| 跨文件一致性 | 7 组通过，8 个破坏引用/重复/版本变更负例被识别 | 双向检查/证据关联、对象范围、根声明、规则与验收版本、完整计数 |
| 独立内容复算 | 7 组通过 | 从合成文件重新读字节，核对开发开始基线、检查输入、当前输入和对象观察及摘要绑定 |
| 合同测试总计 | 14/14 通过 | [完整进程输出](../../../outputs/panorama-process-p0-20260914/contracts-test.log)、[执行时间与环境](../../../outputs/panorama-process-p0-20260914/contracts-run.json) |
| 现有 standalone 回归 | 50/50 通过 | [保存的工具结果](../../../outputs/panorama-process-p0-20260914/standalone-tool-result.json)；中段输出被工具截断，结尾统计与退出码完整 |

合同测试使用 Node 24.19.0、独立测试环境中的 Python 3.12.14、`jsonschema==4.25.1` 与 `rfc3339-validator==0.1.4`。执行了 `node --test test/process/contracts.test.mjs`，并核对它与 `pnpm test:process-contracts` 的注册命令一致。可复现步骤见 [README](README.md)；该测试环境位于工作区输出目录，不安装进业务项目。

standalone 回归执行 `node test/run-standalone-tests.mjs`，通过 `PANORAMA_PYTHON` 指定现有 bundled Python。回归通过后仅补充合同基线、样本、测试和文档；未再修改应用源码。没有运行 build、legacy、浏览器或两业务项目的集成测试：本阶段没有对应运行时/界面改动，这些检查按实施方案留在相关阶段。

**本阶段发现并修正的问题**

第一次合同检查为 12/13：原测试解释器缺少可选日期校验库，`yesterday` 未被拒绝。已建立独立测试环境、固定日期依赖，并使缺少 `date-time` 支持直接报错；另补无效日历日期负例。最后一轮 14/14 包含这些检查。

第一次 standalone 为 49/50：默认找不到 Python，影响 Python AST 回归。显式指定 bundled Python 后为 50/50；未通过修改业务逻辑绕过失败。

交付复核还补齐了开发开始基线与检查前输入的分离，收紧基础必备项不能不适用、current 不携带变化、证据与检查双向引用等约束。基线缺失可以用 unknown 表达，但不能作为 BASE-02 满足的依据。引用与规则计算的完整生产实现属于 P1，测试中的样本检查函数不作为运行时 API。

**来源固定与交付边界**

上游固定为 `9cab2b1345cca68177708fc30f2cac6bbe5792b5`，Harness `0.2.2`、core `2.0.0-preview.2`。编写固定清单时按既有实验归档清单核对 116 个文件，记录 ZIP、文件树和 10 个接口文件摘要，按原字节保存 MIT 许可证。交付复核结果和当前制品摘要见[交付清单](../../../outputs/panorama-process-p0-20260914/delivery-manifest.json)。它记录交付时状态，不是未经采集的开发前全仓库基线。

所有 Receipt 和期望 Assessment 均为显式合成合同样本。负向业务场景本身应当格式合法，用 blocked/unknown 表达缺口；30 个格式负例另在测试中构造。期望结果由人工编排，尚无 P1 评估器自动产生，不能被解释成 cat-sys、霍格沃茨或正式全景新增一次验收通过。

下一阶段 P1 以本包驱动适用性计算、真实输入/对象有效性比较、证据匹配和不可变存储。P2 的开发 CLI 与 Skill、P3 的工作台展示、P4 的自然任务试点均未在本次实施。
