# Persistent project state

For governed work, read `.structure/AGENTS.md` and resolve module IDs through `.structure/manifest.json`. Update affected module facts when their meaning changes. Generated views are rebuilt by structure tools.
