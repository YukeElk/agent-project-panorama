# Project state

This project uses `.structure/`. Read `.structure/AGENTS.md` and relevant module context for governed work. Preserve the selected policy profile and existing user instructions. Use the installed structure tools; do not treat generated Markdown as machine authority.
