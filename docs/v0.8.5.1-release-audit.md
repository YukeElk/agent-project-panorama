# V0.8.5.1 Release Review

日期：2026-08-25

状态：**APPROVED FOR RELEASE EXECUTION — local gates passed; remote CI remains an automatic gate**

候选分支：`agent/v0.8-unified-canvas-flow`

发布基线：`v0.85.0` / `dc32b85`

目标版本：`v0.8.5.1`

## 1. 结论

V0.8.5.1 是 V0.85.0 之后的分层语义补丁发布。它修复了架构层级缺少主视角、归层依据和歧义处理的问题，
并关闭 Studio 按类型/位置猜测层级、跨层后旧依据仍有效、Model/View 丢失空父容器等缺口。实现不包含
DeepSeek、Harness 或 BFF 的生产特例；相关测试只验证名称不能覆盖正式 Assignment。

用户已于 2026-08-25 指定发布版本为 `0.8.5.1`。本地候选通过完整回归、Skill 结构、页面脚本语法和 Diff 检查；
远端 CI 仍是发布完成前的自动门禁。

## 2. 精确候选与验证

| 项目 | 值 |
| --- | --- |
| Representative HTML | `examples/reference-project.v0.2.html` |
| Artifact SHA-256 | `be51f8b589a5f139b240c8c247933e1fd599b83499cc9f5398b1cc9477f943df` |
| Artifact Bytes | `521846` |
| Full pytest | `505 passed, 2 skipped` |
| Layering regression | `6 passed`，包含名称无关归层、Assignment mismatch、Reviewed completeness、空父容器、跨层物化和安全默认 |
| JavaScript syntax | `templates/panorama.html` 与 `templates/panorama-guided-host.html` PASS |
| Skill Creator | PASS |
| Diff hygiene | `git diff --check` PASS |

## 3. 发布 Gate

| # | Gate | 结果 | 证据/边界 |
| --- | --- | --- | --- |
| 1 | Layering Contract / Schema compatibility | PASS | 兼容 Extensions；未冻结新 Core Schema 版本 |
| 2 | Formal Validator | PASS | Profile/Semantics/Assignment/Evidence/Binding 与负向测试通过 |
| 3 | Studio semantic/layout separation | PASS | 跨层归属进入 Semantic Hash；坐标仍只进入 Layout Hash |
| 4 | Model/View ancestor projection | PASS | 空父容器与 `parentGroupId` 保留 |
| 5 | Renderer/Guided Host | PASS | 安全默认、完整层级路径、JavaScript syntax 通过 |
| 6 | Full regression | PASS | `505 passed, 2 skipped` |
| 7 | Remote CI | PENDING | Push 后检查 Python/Node/Browser 自动门禁 |
| 8 | last-good promotion | NOT IN SCOPE | 本次不执行 `verified_delivery.promote` |

## 4. 发布动作

1. 创建 Release Commit；
2. 创建 annotated `v0.8.5.1` Tag；
3. 推送分支与 Tag；
4. 等待并检查远端 CI；
5. 自动门禁通过后创建非 Draft、非 Prerelease GitHub Release；
6. 从精确 Tag 同步本机安装副本并再次执行 Skill 校验。

任一远端 P0 Gate 失败即停止，不创建 GitHub Release。
