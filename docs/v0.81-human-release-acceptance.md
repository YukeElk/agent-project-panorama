# V0.81.0 Human Release Acceptance

状态：`accepted_for_release`

记录时间：`2026-08-23T23:39:47+08:00`

评审者：`thread-user`

记录方式：`natural_language_release_approval_auto_bound_to_unique_frozen_candidate`

用户原文：

- `批准发布为0.81`
- `我就是批准发布就可以了`

## 自动绑定结果

当前发布上下文只有一个冻结且已展示的修复候选。依据
[Approval Gate and Delegated Policy Contract](approval-policy-contract.md)，用户无需复述机器标识；Recorder 将普通发布
批准自动绑定为：

| 项目 | 精确值 |
| --- | --- |
| Release version | `0.81.0` |
| Release tag | `v0.81.0` |
| Delivery | `PVD-2E32A984B6355D411AF96825` |
| Artifact SHA-256 | `b1d28b90a964d3b8ac647617bcc2bedaef98e714f04320ca67120612043efc52` |
| Artifact bytes | `966230` |
| Receipt Hash | `76ce3854447abbd7ddc84c111f31b171674cef87320ca3ea65848cf33c8a28c6` |
| Human release decision | `accepted` |

该授权覆盖将上述唯一候选作为 V0.81.0 代表制品、创建 Release Commit、annotated Tag、推送和创建非 Draft、
非 Prerelease GitHub Release。它不授权修改业务项目、不自动晋升本地 last-good，也不扩大任何 Approval Policy。

## 证据分层与已披露边界

Verified Delivery 的 Model/View、Geometry、Privacy 与 Offline Security Gate 均为 `passed`。本轮受本地
`file://` 浏览器控制安全边界影响，没有为修复字节形成新的 Browser Evidence；Receipt 如实记录
`browser.status=not_provided`、`informationGaps=["browser_evidence_not_provided"]`，机器 `visualReview` 保持
`pending`。

本文件记录用户对唯一冻结候选的人工发布判断，不反写或伪造机器 Browser/Console/Viewport 事实。历史 V0.8
候选的浏览器矩阵仍只作为回归基线，不能冒充本候选的新机器证据。

## 失效条件

候选字节、版本目标或已披露风险发生变化时，本记录自动失效；存在多个候选时不得推断选择。仅 Release Commit
对文档和版本元数据的确定性封装不改变上述代表 Artifact 字节。
