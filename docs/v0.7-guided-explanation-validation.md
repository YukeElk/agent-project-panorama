# V0.7 Guided Visual Explanation 验证报告

日期：2026-08-22

状态：**Machine Gates Passed；Exact Artifact Human Acceptance Pending**

对应 Finding：SF-57

## 1. 验证结论

V0.7 已形成单一、可重算的 `panorama-explain-pack.v0.1`，并由同一包驱动：

1. Codex 对话内 HTML Fragment；
2. 同源 Markdown 降级；
3. Panorama Multi-view Renderer 0.3 原生讲解 Dialog；
4. Verified Delivery 的传递 Hash Binding。

机器验证没有发现 Schema、Binding、Determinism、Privacy、Offline Security、Geometry、Viewport、Console 或
V0.6 Backward Compatibility 失败。机器状态没有被提升为人工状态；代表性候选仍为
`visualReview=pending`。

## 2. 自动化结果

- Skill quick validation：passed；
- Python compile：6 个新增/修改入口 passed；
- 全量 pytest：`419 passed, 2 skipped`；
- V0.7 专项：11 tests passed，覆盖六类 View、Governance-only、Event Checkpoint、Determinism、Tamper、CLI、
  Codex Fragment、Markdown、Native Dialog 与 Verified Delivery；
- V0.6 兼容：无 Explain Pack 时继续生成 `panorama-multi-view-bundle.v0.1`；V0.6 Renderer、Guided View Set、
  Verified Delivery 与 Policy Adapter 回归通过；
- `git diff --check`：passed。

负向门禁包含：Pack/Claim/Core Digest/Project Binding 篡改、缺少 Panorama Core、View Set/Layout Binding 漂移、
重复输出、Fragment 写入仓库、Document Shell/Network 能力、超过容量与 Verified Delivery 不匹配。

## 3. 五 Case Proof Lab

| Case | 重点 | Explain Pack | Story / Step | 结果 |
|---|---|---|---:|---|
| Controlled Orders | 六 Profile、Current/Target、Decision、7 Transition、Verification | `PEP-5D063F4C6E7191B4172BE8B4` | 17 / 48 | passed |
| Spring PetClinic slice | 大型 Java 静态依赖切片、单 View 降级、Gap 保留 | `PEP-73C7D97E00EA45216CE67F74` | 3 / 8 | passed |
| Backstage slice | 多列依赖图、障碍路由、单 View 降级 | `PEP-DEEB96095634CF9158F7C111` | 3 / 8 | passed |
| Governance-only fixture | Decision、Requirement→Acceptance/Gate、无 Event 不补 Sequence | `PEP-6E21968BDA462EAB08C803D0` | 13 / 47 | passed |
| Event Checkpoint fixture | Event Action Sequence、Lifecycle before/after、禁止 Runtime Call 提升 | `PEP-BCD22FFDC5BC2778DA07CF1A` | 13 / 51 | passed |

PetClinic 与 Backstage 的真实浏览器交互均成功打开讲解 Dialog，Story 数为 3，Geometry Diagnostics 的
node overlap、edge-through-node、side overflow 均为 0，Console error/warning 均为 0。

## 4. Codex Fragment 验证

Controlled Fragment：

- Explain Pack：`PEP-5D063F4C6E7191B4172BE8B4`；
- 字节：84,619；
- SHA-256：`3cbc873c3ad9c20a69f7e258482f6ddfd8838fb22cfda006eedba3d51b58f485`；
- 结构：单一 `<section>` Root，无 `doctype/html/head/body`；
- 安全：无 `fetch`、XHR、WebSocket 或远程资源；
- 交互：Story Select、上一步/下一步、Step 文案、Node/Edge Focus 同步；
- 可访问性：语义按钮/Select、SVG title/desc、`aria-live=polite`；
- 视口：320、360、736px 实际浏览器检查无水平溢出，Console 0/0；
- 降级：同一 Pack 可确定性生成 Markdown。

Fragment 只输出到 Codex 任务专属可视化目录，不进入发布仓库或目标项目。它不复制或硬编码 `visualize`
插件源码、资产或缓存路径。

## 5. Panorama Dialog 与 Browser Evidence

受控候选：

- Delivery：`PVD-DB2B0BB69CF50F4BBB034917`；
- Explain Pack：`PEP-5D063F4C6E7191B4172BE8B4`；
- Bundle：`PVB-EAB2911DA3DBEF89C03993CA`；
- Artifact bytes：162,371；
- Artifact SHA-256：`fd47f081eb15c00cb2bf59cea7f4bcda80715e74eae9dd047852c3a319a48e38`；
- Browser Evidence：`PBE-77600089EB51AA4D26245A0D`；
- Evidence Hash：`631b5e04c2c585d6985d070658f7ea6193bde3ec896c80449a6bd1211d4cae23`；
- Receipt Hash：`23883fc255f20741115795742abb53b345c64cb633ccb469cb355e6af417b180`；
- last-good modified：false。

浏览器证据对 1440×900、1600×1000、1920×1080、2048×1320 四档逐一遍历 7 个 Chapter，再打开
Dialog 并推进到第二步。四档均为：document X/Y overflow 0、node overlap 0、edge-through-node 0、side
overflow 0、Console error/warning 0。另在 360px 验证 Dialog 无水平溢出；桌面端使用右侧停靠轻遮罩，窄屏
回到居中弹窗，使当前 Step 的图上高亮保持可见。

## 6. 剩余发布门禁

当前只剩独立人工视觉 Acceptance、提交/远程 CI、Tag/Release 与精确安装验证。人工评审必须明确绑定上面的
`PVD-DB2B0BB69CF50F4BBB034917` 和 Artifact SHA-256；任何 Renderer/Template/Pack/Artifact 字节变化都使接受
失效并要求重新生成候选与评审。机器 Browser Evidence 继续保持 `visualReview=pending`，不得反写为
`accepted`。
