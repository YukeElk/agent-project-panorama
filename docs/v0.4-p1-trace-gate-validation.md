# V0.4 P1 Traceability & Gate Projection Validation

日期：2026-08-13

范围：Drawer Trace Route、Trace Gap Candidate / Formal Finding 分离、Gate / Acceptance /
Deployment 状态投影，以及 Architecture Studio 的 Proposal Patch / Affected Entities /
Validation / `change_ready` 投影。

## 实现合同

- 只使用 Panorama Schema 已存在的 ID 引用；每条边携带字段 JSON Pointer。
- 多跳 Route 保留全部中间实体与每一跳 Pointer，不生成虚构直接边。
- Renderer 派生缺口固定标记 `Trace Gap Candidate`；Formal Finding 仅呈现实际 Finding Code。
- `verified / accepted / deployed_observed / change_ready` 均为只读派生；证据不足时为
  `blocked / unknown`，不写入正式 Panorama。
- Bridge 的 Proposal 响应直接返回 `propose_update.py` 的 operations、affectedEntities 和
  validation；Renderer 逐项呈现，不创建第二套 Diff。
- 未修改 Panorama Schema、正式 `project-panorama-data` 或三个主视图。

## 自动检查

- 全量测试：`253 passed in 67.32s`。
- Renderer JavaScript：Node `--check` 通过；URL Sanitizer 测试通过。
- `quick_validate.py`、两个 Reference Validator、`git diff --check` 均通过。
- v0.1 / v0.2 Reference Payload 与各自 JSON 完全一致。
- Template 与两个 Reference 的 Presentation Hash 均为
  `d6b9dfa8ac46e27d9fcdb5f3f1e56b716535c22a267e3b267ebcbe6197118bb7`。

## 浏览器检查

- 受保护 Bridge launcher → document → Studio 链路通过。
- Requirement / Module / Gate Drawer 可展示精确 Trace Route、JSON Pointer、缺口候选与治理状态。
- Studio Formal Validation 为 0 error，并可展示 Bridge 返回的真实 Proposal operations、
  affected entities、validation 和 `change_ready` 条件。
- 复测连接/保存期间继续语义编辑：旧响应不会覆盖最新本地草稿。
- 1024px 桌面与 390×844 移动视口无页面横向溢出，移动端 Studio 分区可用。
- 最终浏览器控制台：0 warning / 0 error。

## 边界

- Trace Gap Candidate 不是 Formal Finding，也不能证明缺口不存在。
- 状态投影不替代人工批准、验收、部署观察或 Formal Validator。
- P1 不实现 Mission DAG、Agent 调度、通用评测、Git Promote 或第四主视图。
