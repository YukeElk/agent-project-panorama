# V0.6 Guided Multi-variant View Set 验证记录

日期：2026-08-21

状态：Machine Closed；Independent Visual Pending

## 结果

- 新增 `panorama-view-set.v0.1` Schema、确定性 Compiler/Validator 与 CLI；
- Renderer 0.2 允许同一 Profile 的多个单 scope Variant，并由一个 View Set 同时驱动章节栏、顺序、标签、
  默认章节和深链；
- 受控模型同一候选包含“模块 · 当前”和“模块 · 目标”，二者分别为 7 nodes / 6 edges；目标视图显示
  Metadata Store，当前视图显示 Direct Vector Access，未合并 Scope；
- `Project Console` 选择从 Current 切换到 Target 后保持；右方向键从 Target 到 Dependency；深链稳定绑定
  `VIEW-747E80F00994781985572228`；
- 四档桌面（1440×900、1600×1000、1920×1080、2048×1320）和两档窄屏（839×900、430×820）均通过
  7 章节遍历；最大 node overlap / edge-through-node / side overflow 均为 0，横向页面溢出为 false，Console
  为 0 error / 0 warning；窄屏纵向滚动属于预期布局；
- 浏览器诊断现在通过隐藏只读输出绑定当前 View Set/Chapter/View 和几何结果，方便外部 Browser Gate 读取，
  不写回 View IR；
- Verified Delivery `PVD-8A3BABDA1D3ACC459A5216DC` 绑定 View Set
  `PVS-14D5F17F795D17961F2DB518`、Browser Evidence `PBE-463EEAC60E89DA9750D5C497` 和 Artifact SHA-256
  `f57c531e6cc6c573f1a90cd287d6f6e5277ea061a8ebbc9f95061a7a125df459`；last-good 未修改，
  `visualReview=pending`。
- 完整回归为 `401 passed, 2 skipped`；Runtime Preflight 为 24 Schema / 0 error；Skill Validator passed。两个
  Skip 仍是 Windows 符号链接环境分支；Browser plugin 已在真实浏览器补充验证 Renderer 交互与 Console。

## Fail-closed 覆盖

- 重复 View ID、超过 24 章节、多 scope View、非连续 Order、重复 Chapter/View、集合未精确覆盖、Model/View
  Binding、View Semantic/Layout Hash、View Set ID/Hash 与 Chapter ID 任一篡改均拒绝；
- Verified Delivery 收据显式绑定 View Set；旧 v0.1 收据的缺省字段继续可验证，但所有新候选都会写入绑定；
- View Set 没有 Topology/Layout 字段，不能成为第二套展示拓扑；Conflict 没有正式事实时不生成伪章节。

## 仍未关闭

- 这是受控事实模型验证，不替代支持语言公开项目门禁；Java Adapter 仍未实现；
- 当前导航标签为确定性 Profile × Scope，不含 Archify 式自由 Story 文案/动画；自由 Story 若后续加入，必须仅引用
  现有实体与关系；
- 当前图形布局在信息抽屉打开时会压缩主画布，视觉完成度仍需独立人工评审；机器截图不能代替接受；
- 需在最终 Commit 上重跑 Panorama × Archify Proof Lab、受支持公开项目和完整发布审计。
