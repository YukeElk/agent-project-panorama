# V0.6 Event Projection Validation

日期：2026-08-21  
状态：Event Checkpoint + Sequence/Lifecycle/Evolution-Risk 编译切片通过；尚未发布

## 已证明能力

- current Event Chain → hash-bound Checkpoint → Model IR；
- Actor → Subject Engineering Event Action Sequence；
- explicit before/after Lifecycle Transition；
- Event Hash Chain Evolution Timeline 与 failed/rejected 风险强调；
- Project、Tail、Sequence、Event Hash、correlation 和 Evidence Pin 绑定；
- Model/View CLI 端到端编译；
- Governance Proposal Apply 与 Approval Policy Execution Producer 输出受约束 lifecycle transition。

## 负向证明

- Event 内容或 Checkpoint Hash 篡改拒绝；
- Head behind/missing/mismatch 不形成 Checkpoint；
- Project mismatch、无 Subject、无显式 lifecycle transition 保留错误或 Information Gap；
- Sequence 不宣称 Runtime Call，Lifecycle 不从 Outcome 猜状态，Risk 不宣称 Formal Finding 或 blast radius。

## 当前验证

- V0.6 Event/Model/View 聚焦组合：`31 passed`；
- Event Producer 与 Approval/Event Head 回归组合：`42 passed`；
- Event + Producer + Model/View 扩展组合：`67 passed`；
- 完整回归：`380 passed, 2 skipped`。

20 个 Schema 通过 Runtime Preflight；Skill Validator、URL Sanitizer、Python 编译和 `git diff --check`
通过。Preflight 的通用 PATH 未发现 Node，但工作区绑定 Node 的 Sanitizer 已独立通过；Renderer 发布环境仍须
重新验证 Node 可发现性。本报告不代表 Renderer、Verified Delivery、Proof Lab 或 V0.6 Release Gate 已闭合。
