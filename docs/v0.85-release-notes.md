# V0.85.0 Release Notes

日期：2026-08-24

状态：**Approved Release — external publication remains subject to automatic CI gates**

## 新增能力

- 多语言 Source Observation/Mapping v0.2，保留 v0.1 输入兼容；
- 唯一版本化 Multistack Adapter Registry 与机器可读 Support Matrix；
- 正式 L1：Python、JavaScript、TypeScript、Java、Kotlin、Go、C#；
- Preview L1：Rust、PHP、Ruby、Swift、Scala、C、C++；
- framework/data/AI/interface/delivery `stackProfile` 和 Monorepo/Build Boundary；
- 多语言 Source-to-Module Candidate，不自动提升正式 Module；
- 项目本地幂等 Source Sync 与不可变 Observation/Receipt/Loss；
- Source v0.2 → Model IR → Dependency View → Explain Pack 贯通；
- CI 新增 Node Renderer Test、固定 Capability Matrix 和七档 Browser Matrix；
- 统一画布只对真实存在子画布的模块显示“双击查看内部逻辑”，不再误导不可下钻节点；
- Windows CLI 对 stdout/stderr 统一 UTF-8，消除本地失败分支的乱码/捕获异常。

## 固定能力矩阵

- Expected/Matched Facts：42 / 42；
- Supported Fact Recall：1.00；
- Evidence Coverage：1.00；
- Forbidden Claim Avoidance：1.00；
- Determinism：PASS；
- Registry：`panorama-multistack-adapter-registry 0.85.0` / `77005e530ce7f1a7704323911b59621fbfcaf2703523668469c95ad8b3a55dfc`。

## 保留边界

- L1 不是完整 AST、Type Checker、Call Graph、Module Logic 或 Runtime Trace；
- Preview 语言不得在 UI/文档中称为正式支持；
- Current 模块内部逻辑仍由受约束的 Codex 源码归纳 Sidecar 产生，不由 import 自动推导；
- `embeddedSecretScan=not_performed` 保持公开披露；只证明分类 Secret 路径未读、源码正文未持久化；
- 团队协作、RBAC、中央服务、Dataset/RAG/Eval Export 不在 V0.85 发布范围；
- last-good 晋升不属于本次发布，仍需独立的 `verified_delivery.promote` Policy 与批准。
