# V0.85 Human Release Acceptance

日期：2026-08-24

批准原文：`批准发布`

## 自动绑定的唯一冻结候选

| 项目 | 值 |
| --- | --- |
| 目标版本 | `v0.85.0` |
| 候选分支 | `agent/v0.8-unified-canvas-flow` |
| 基线 | `v0.81.0` / `ea3c3c7cc3aaea0d3c2e74ae7e90308f69f357ce` |
| Representative HTML | `examples/v0.8-unified-canvas-reference.html` |
| Artifact Bytes | `975621` |
| Artifact SHA-256 | `6c65b0c7d5d45e185e4f46997285cebbf50d40afe33aceac7e2e5aa17dacf46c` |
| Adapter Registry | `panorama-multistack-adapter-registry 0.85.0` |
| Registry SHA-256 | `77005e530ce7f1a7704323911b59621fbfcaf2703523668469c95ad8b3a55dfc` |

批准时只有一个已冻结并已展示的 V0.85 候选，版本、代表性交付物字节和已披露风险均未变化。因此依照
`docs/approval-policy-contract.md`，自然语言批准被自动绑定到上述候选，不要求用户再次抄写 PVD 或 Hash。

本文件和 Release 文案属于批准/发布元数据，不改变已审批 Representative HTML、能力实现或风险边界。

## 授权范围

本次批准授权：

- 创建 Release Commit；
- 推送候选分支；
- 等待并检查远端 Python/Browser 自动门禁；
- 自动门禁通过后创建并推送 annotated `v0.85.0` Tag；
- 检查精确 Tag 的远端门禁；
- 创建非 Draft、非 Prerelease GitHub Release；
- 从精确 Tag 执行隔离安装与 Skill 校验。

以下动作不在本次批准范围：

- `verified_delivery.promote`；
- 修改或晋升 last-good；
- 团队协作、RBAC、中央服务或 Dataset/RAG/Eval Export 扩展。

任一自动 P0 Gate 失败时，本次发布自动停止，不以人工批准覆盖失败结果。
