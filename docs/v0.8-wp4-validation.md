# V0.8 WP4 Unified Read Canvas Validation

日期：2026-08-23

状态：WP4 机器与浏览器专项通过；不是 PVD、发布批准或人工视觉接受

## 实现结果

- Integrated Bundle 新增独立 `childViews[]`；`module_logic` 不进入顶层 Guided View Set。
- `阅读模式` 使用唯一 `CanvasSceneAdapter / CanvasStore / CameraController / NavigationStack`。
- 架构 Module 双击下钻，Module Logic 子画布只显示内部节点、Boundary Port 和紧凑外部引用。
- 单击节点直接显示内部实现逻辑；不要求回车。
- Canvas Viewport 使用 `overflow:hidden`，支持空白拖动、滚轮/按钮缩放、Fit All/Selection。
- 父子 View 分别保存相机；Back 恢复父画布离开前的 `x/y/scale`。

## 自动验证

- WP4 最终全量：`462 passed, 2 skipped in 201.10s`。
- WP4 Renderer 回归：`16 passed`。
- WP4 Parent/Child 专项：`4 passed`。
- Guided Host JavaScript 由 Node `new Function(...)` 完成语法解析。

两个 skip 仍是 Windows 符号链接权限覆盖，不是 V0.8 功能跳过。

## 浏览器证据

目标：`http://127.0.0.1:8788/.panorama-work/v0.8-wp4-browser/unified-parent-child.html`

Codex In-app Browser 验证结果：

1. 页面标题为 `Agent 项目全景`，页面非空，无框架错误覆盖层，Console `error/warn` 为 0。
2. `系统 → 阅读模式` 只挂载一个可平移缩放 Canvas World；旧独立 `全景讲解` 模式不存在。
3. Zoom 将父相机从 `translate(35,105.22) scale(.509266)` 更新为
   `translate(-33.9469,74.4361) scale(.611119)`。
4. 空白拖动后相机变为 `translate(66.0531,19.4361) scale(.611119)`，没有使用画布滚动条。
5. 双击 `MOD-ORCHESTRATOR` 进入 4 节点/3 关系子画布；只含两个内部节点、一个边界端口和
   `MOD-CONSOLE` 外部紧凑引用。
6. 单击“规范化请求”立即显示用途、实现类型、设计原因和内部实现逻辑。
7. Back 返回父画布后相机精确恢复为
   `translate(66.0531,19.4361) scale(.611119)`。
8. 430×900 响应式视口仍可看到阅读模式、Canvas、相机控件，Console 无 warning/error。

## 已知边界

- 当前浏览器 Candidate 使用 Target Design 验证父子导航；Current Observation 已通过 Model/View 专项测试，
  仍需在 WP8 三项目浏览器矩阵中验证。
- 代表项目 Layer/Module 尚未补齐 authored 中文 `displayName`，因此父画布仍显示英文技术名；WP7 必须修复。
- Studio 仍是旧编辑表面，尚未迁入统一 Canvas Runtime；属于 WP5。
- Module Logic 子画布 Explain/Progress/Risk/Decision Story 尚未编译；属于 WP6。
- 本文不生成或批准 Verified Delivery Candidate，不允许发布或 last-good Promotion。
