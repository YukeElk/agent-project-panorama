# V0.6 Semantic Source & Module Mapping Validation

状态：Machine Gate Passed；Release 仍为 Partial

## 结论

固定 Spring PetClinic `88e37c15cf6fc8490b01bc3e8e2c800cec1ac272` 切片已证明 Source Extractor 不只生成
import topology，还能持久化有行级 Evidence 的 Java 声明/注解/构造参数、Maven dependency 和脱敏 Properties
声明。tool-native normalizer 在 10 个 reviewer oracle facts 上得到：

- supported：8；partial：1；contradicted：0；unmapped：1；
- supported fact recall：`0.80`；
- forbidden-claim avoidance：`1.00`；
- Secret canary：未检出。

因此 `PUBLIC_SOURCE_SEMANTIC_COVERAGE_AND_MODULE_MAPPING` 机器门禁关闭。它不等于“完整 Java 语义理解”：
Thymeleaf 只有 manifest declaration，尚未解析 Controller return → Template；Service comment conflict 尚未进入
Observation。

## Source Binding

- 上游 Inventory：63 files，Inventory SHA-256 `098e45cb...`；
- 支持并读取：49 Java + 3 Properties + 1 Maven POM；
- Content Digest：`4b56c804...`；coverage 为 supported input 完整；
- Observation：`SRCOBS-1C7EFC8ECDD6219697250BA6` / `c1a1e926...`；
- 255 elements、498 relations、0 unresolved、0 parse failure。

Properties 只持久化 key/line/Evidence Pin 与白名单安全值。datasource URL 只保留 `jdbc:<driver>`；密码、用户名、
token 和任意自定义值保持 `omitted_by_policy`。Configuration 不能提升为 active runtime。

## Module Mapping

`MAP-1AE751BC39685FA27786539A`（Hash `76efd59d...`）生成 5 个 main-source Java package 候选，覆盖 30 个
Source Element；23 个 test/config/manifest Source Element 保持 unmapped。输出固定：

- `status=pending_review`；
- `promotionPolicy=exact_approved_mapping_proposal_required`；
- 每个 candidate `reviewStatus=pending`；
- `suggestedModuleId=null`；
- 正式 Module 新增数为 0。

这证明可探索 package-based responsibility candidates，同时保持“package ≠ Module”。只有评审职责、接口、状态所有权、
部署边界后，才允许通过正式 Panorama Proposal/Approval/Apply 采纳。

## 剩余发布门禁

1. 在最终 commit 上重跑多项目 Proof Lab、全量测试、Schema Preflight、Skill Validator 与 Renderer/Browser Matrix；
2. 精确最终 HTML hash 的独立人工视觉 Acceptance；机器 Browser Evidence 不能代写为 accepted；
3. 通过发布审计后再执行安装、tag、push 与 public release。

机器记录：[`validation-lab/v0.6-semantic-source-module-validation.v0.1.json`](../validation-lab/v0.6-semantic-source-module-validation.v0.1.json)
