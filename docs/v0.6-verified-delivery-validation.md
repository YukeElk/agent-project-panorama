# V0.6 Verified Delivery Validation

状态：WP6 Machine Slice Passed；Proof Lab / Independent Human Visual Acceptance Pending

日期：2026-08-21

## 已闭合能力

- 六视图候选通过 Model/View、Geometry、Privacy、Offline Security 与 hash-bound Browser Evidence 后，生成
  immutable Candidate、Evidence 与 Delivery Receipt；准备阶段不创建或修改 last-good。
- `verified_delivery.promote` 只接受精确批准、当前有效、单 Operation 的 Approval Policy；Profile 固定包含
  semantic/schema/geometry/browser/privacy、允许内部 pending visual、拒绝 rejected、原子替换、禁止 Core 修改
  与外部发布。
- Promotion Transaction 在分配 Use 前冻结 Candidate Receipt、Artifact Hash/Bytes、last-good Before/Expected
  与 Policy Hash；每次状态变更重新计算 `stateHash`。
- `post-effect/pre-receipt` 中断从 `effect_observed` 恢复审计，不重复替换；第三状态不覆盖、不 rebase；成功
  必须同时绑定 Engineering Event、Policy Execution Receipt 与 Ledger。
- 相同 Candidate 已是 last-good 时返回只读 no-op，不分配 Policy Use。

## 真实 Renderer Evidence

当前 self-host 候选 `PVD-AFF938BFD8CB3E2E74378634` 绑定：

- Artifact SHA-256：`37341fa9df5b1906de2a5ae15185b5b43209357a259f41285aeb52ff479c3185`；
- Browser Evidence：`PBE-DFE97D44AE1056C0F69E9ABD`；
- 四档 Desktop：1440×900、1600×1000、1920×1080、2048×1320；
- 两档窄屏：839×900、430×820；窄屏仅有预期纵向滚动；
- 所有视口：0 横向溢出、0 节点重叠、0 连线穿节点、0 侧栏越界；Console 0 error / 0 warning；
- 独立人工视觉状态：`pending`。

## 故障注入

聚焦测试覆盖正常晋升、no-op、候选/事务篡改、浏览器证据错配、缺失 viewport、overflow、分配后中断、
效果后中断、Use 分配后候选漂移且无外部效果、last-good 第三状态与原 last-good 保留。聚焦结果：`10 passed`。

完整回归第一次在更新后的本地 Python Runtime 上得到 `385 passed, 2 skipped, 9 failed`；9 项均由运行环境
缺少 PyYAML 或 Windows 子进程未启用 UTF-8 引起，并非产品回归。项目本地临时依赖补齐并设置
`PYTHONUTF8=1` 后，失败集合 `11 passed`；事务语义收紧并新增候选漂移测试后的最终完整回归为
`395 passed, 2 skipped`。Runtime Preflight 识别
23 个 Schema，全部兼容；Skill Creator Validator 通过。Node 未出现在当前 PowerShell PATH，Node 相关验证沿用
WP4 已完成的 bundled runtime/Browser 证据，本轮未将其误报为当前 Preflight available。

## 未闭合边界

- 尚未执行 Panorama × Archify 的当前 V0.6 同项目 Proof Lab；
- 尚无独立人工对精确 Artifact Hash 的 `accepted`；
- 尚未执行安装、Tag、Push 或公开 Release；
- Dataset/RAG/Eval/Post-training 仍是 V0.7 设计议题，不属于本切片。
