# V0.6 Source Extraction Slice A 验证记录

验证日期：2026-08-21

状态：**Local Development Slice Passed；不是完整语言分析、多图或发布认证**

分支：`agent/v0.6-multi-view-architecture-panorama`

前一里程碑：`04848ee`（Model/View IR Foundation）

## 1. 验证链

```text
Bounded Repository Inventory
→ Python AST / JS-TS bounded static / package.json / pyproject adapters
→ panorama-source-topology-observation.v0.1
→ Extraction Receipt + Transformation Loss Report
→ optional Panorama Core + Source Observation Model IR
→ existing single-scope Module View remains isolated
```

## 2. 机器合同

- `source-topology-observation.schema.v0.1.json`；
- `extraction-receipt.schema.v0.1.json`；
- 复用 `transformation-loss-report.schema.v0.1.json`；
- Model IR `source_element` Entity Kind 与严格 Source Observation Binding。

Runtime Preflight 当前检查 20 个 Schema，均为 `compatible`。

## 3. 聚焦验证

`tests/test_v060_source_topology.py` 与既有 Model/View 测试合计：**20 passed**。

覆盖：

- Schema meta-validation 与相同 bytes/version/as-of 的确定性输出；
- 非 Git Content Digest、Project Binding、Inventory、Observation/Receipt/Loss Hash；
- Python AST 的绝对/相对/type-only/dynamic import；
- JS/TS 的 comment-aware static/type/require/dynamic import；
- `package.json` 与 `pyproject.toml` declared dependency；
- unresolved/dynamic、full-parser 缺失、parse failure 的 Loss/Information Gap；
- `.env` Secret-risk 路径排除，源码正文与 Secret 值不进入制品；
- 普通源码瞬时读取与未执行 embedded-secret scan 的真实 Receipt 声明；
- 单文件容量上限、Observation/Receipt 篡改、Project mismatch 与覆盖拒绝 fail closed；
- Source Element 合并后仍不是 Module，Dependency 没有 runtime/sequence order；
- 既有 Current Module View 仍为 7 nodes / 6 edges，不吸收源码候选拓扑。

## 4. Self-host Smoke

在本项目当前工作树执行只读抽取：

| 指标 | 结果 |
| --- | ---: |
| Included files | 87 |
| Source elements | 170 |
| Typed relations | 819 |
| Unresolved/dynamic relations | 3 |
| Receipt / Loss | `partial` / `partial` |
| Information Gaps | 3 |

`partial` 是预期真实性状态：存在 3 条动态或未解析关系，并且 JS/TS 内置适配器不是完整 parser。该结果不等于
架构关系全覆盖，也不表示 819 条静态依赖是运行时调用。

## 5. 完整回归

完整 pytest：**369 passed, 2 skipped in 127.84s**。

两个 Skip 仍为既有 Windows 环境不依赖特权创建符号链接的测试路径；本轮新增的 link/reparse 策略使用确定性
故障注入验证，没有增加 Skip。

## 6. 安全结论

- 未调用网络、未执行项目代码、未安装依赖、未修改业务项目；
- 已分类 Secret 路径、越界路径和 in-scope link/reparse path 不读取；
- 普通源码只为 parser 瞬时读取，正文不持久化；
- 本轮没有扫描普通源码中的嵌入式 Secret，因此不提供“源码无 Secret”证明；
- 文件数、单文件大小、总字节与路径深度使用 fail-closed 上限。

## 7. 尚未验证

完整 JS/TS parser、Python symbol/call graph、path alias、build/bundler resolution、container/CI/deployment 配置、
作者模型、runtime trace、Dependency/Data Flow View Compiler、Renderer、Browser、Geometry 与外部发布均未完成。
