# V0.4 P0B Studio Journal 验证记录

状态：P0B 通过

## 实现范围

- Bridge 权威完整 Semantic/Layout Hash；离线模式为 `not_computed_by_bridge`；
- Semantic/Layout 双轨 Session Operations 与 before/after 展开记录；
- 候选字段级 Semantic Diff，排除坐标和操作时间；
- 语义字段影响提示、候选假设/未知项、数据流协议/通信/流向/可靠性编辑；
- semantic edit、candidate switch、Panorama/Data/Git/Source drift、CAS、coverage 与 formal binding
  等具体 stale 原因的多值披露；
- 候选新建、克隆和重命名使用原生 Dialog。

未修改 Panorama Schema；正式数据仍只可通过 Bridge Proposal/Approval/Apply 更新。本里程碑未实现
P0C 完整键盘/焦点模型、P1 Trace Projection 或 P2 Verification Receipt Adapter。

## 自动测试

- P0B Session/Renderer 定向回归：24 项通过；
- 全量回归：240 项通过；
- Renderer JavaScript 语法检查与 `git diff --check` 通过；
- Reference v0.1/v0.2 JSON payload 保持不变，Presentation Hash 与模板一致。

## 浏览器检查

受控本机 Bridge、随机 loopback 端口和 Reference v0.2 临时副本：

- 进入 `系统 → 逻辑架构 → 架构设计 Studio`，双 Hash 均显示完整 64 位 Bridge 权威值；
- 使用原生 Dialog 克隆候选，编辑模块用途后，Semantic Hash 改变、Layout Hash 保持不变；
- 勾选两候选后显示 `MOD-CONSOLE.purpose` 的 before/after 字段级 Diff，坐标明确排除；
- Semantic Operations 显示 `node.update` 及 actor、时间和语义影响；
- 同时显示 `candidate_semantic_changed` 与 `formal_source_uninitialized`；
- 390×844 响应式视口中候选 Diff、双 Hash 和 Operations 仍可访问；
- 新版独立页面 console error/warning 为 0。

## 验收结论

P0B 满足已批准合同：它增强现有 Architecture Studio 的可观察性，不建立第二套 Studio，不修改正式
Panorama 数据或 Schema，不新增主视图，也不把 Session Operations 冒充不可变审计 Journal。
