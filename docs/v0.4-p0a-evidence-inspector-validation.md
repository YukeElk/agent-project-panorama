# V0.4 P0A Evidence Inspector 验证记录

状态：P0A 通过

## 实现范围

- `SYSTEM → 架构来源` 三栏 Evidence Inspector；
- Fact Provenance、Observation Batch、Conflict Summary、Current Architecture Snapshot 与正式 Reference 投影；
- 离线 `currentness_unknown` 和 Bridge 实时 `match/drift/uninitialized/incomplete`；
- 来源 Hash/覆盖摘要、具体漂移原因与重新检测入口；
- Schema 0.1 空状态与原架构基线兼容。

## 边界

- 未修改 Panorama Schema；SF-24、SF-25 已记录。
- 未实现 P0B Studio Operations/Semantic Diff、P0C 可访问性全量、P1 Trace Projection 或 P2 Receipt Adapter。
- Renderer 不裁决 Conflict，不把枚举 Confidence 换算为分数，不把离线 HEAD 表述为当前。

## 自动验证

- Python 全量回归：234 项通过。
- V0.4 P0A + Bridge 定向回归：22 项通过；Renderer JavaScript 语法检查通过。
- URL Sanitizer Node 测试通过；Skill `quick_validate.py` 通过。
- v0.1/v0.2 Reference HTML 均为 0 Error；已知 Warning 是 Reference 项目的显式风险样本。
- 两份 Reference 的完整 JSON 与源文件相同，Presentation Hash 均为
  `de9f0bf80b8dd3e5b4fd6e14a412510448980cc50967beb3960c5ee274cdb48e`。

## 浏览器验证

使用临时忽略文件、真实 Continuous Observation、随机 `127.0.0.1` Bridge 与应用内浏览器验证：

1. capability Launcher 成功载入受保护 Renderer；
2. `系统 → 架构来源` 显示 6 条 Fact Provenance、1 个 Observation Batch 和 `MATCH` 实时状态；
3. 按 Evidence 文本筛选后只保留 1 条记录，选中事实后右栏 JSON Path、Formal/Actual HEAD、
   Snapshot、Content Digest 和覆盖计数同步更新；
4. “重新检测来源”调用受保护 Context 并更新时间；
5. 桌面与 390×844 移动视口均无页面横向溢出，移动端三栏按单列展开；
6. 两个视口的相关 console error/warning 均为 0。

截图保存在系统临时目录：`panorama-v04-evidence-desktop.png`、`panorama-v04-evidence-mobile.png`。

## P0A 验收结论

P0A 满足已批准合同：Evidence 与 Architecture UI 回流已实现，不新增主视图，不改 Schema，
不把离线记录称为实时事实，也不引入通用 Run/Mission/Trial 平台。P0B、P0C、P1、P2 继续按既定顺序实施。
